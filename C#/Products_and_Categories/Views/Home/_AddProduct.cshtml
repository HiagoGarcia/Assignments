@model Association

            <div class="row mt-5">
                <div class="col-md">
                    <div class="card">
                        <div class="card-body">
                            <h3>Categories!</h3>
                            <form asp-action="AddProductToCategory" asp-controller="Home" method="post"
                                asp-route-categoryID="@Model.CategoryID">
                                <div class="space">
                                    <label>Categories</label>
                                    <select asp-for="@Model.ProductID">
                                        @foreach (var prod in ViewBag.UnrelatedProducts)
                                        {
                                            <option value="@prod.ProductID">@prod.Name</option>
                                        }
                                    </select>
                                </div>
                                <input type="submit" value="Add Category">
                            </form>
                        </div>
                    </div>
                </div>
            </div>
